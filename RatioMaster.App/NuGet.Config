<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Stable Avalonia (12.0.x) + 2ndLAB.SkiaSharp.Static are all on nuget.org. The Avalonia nightly feed
         was only needed for the 12.1 alpha, which was reverted (that nightly crashes on Android). -->
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
  </packageSources>
</configuration>
